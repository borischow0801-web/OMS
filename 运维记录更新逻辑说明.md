# 运维记录更新逻辑说明

## 一、更新逻辑概述

**重要说明：运维记录的更新逻辑不调用任何外部接口（包括Bspplus接口），只从本地数据库选择承建方用户。**

## 二、更新流程

### 1. 前端流程

1. 用户在前端选择协助人（从下拉列表中选择）
2. 下拉列表的数据来源：`/api/accounts/users/contractors/` 接口
   - 该接口只返回 `role` 为 `manager` 或 `employee` 的用户
   - 过滤掉当前登录用户
3. 提交更新时，前端发送的数据包含：
   - `assistant`: 协助人用户ID（整数）或 `null`
   - 其他字段的正常数据

### 2. 后端流程

#### 2.1 权限检查
- 检查当前用户是否是承建方（`manager` 或 `employee`）
- 如果不是，返回 `403 Forbidden`

#### 2.2 数据验证
- 使用 `MaintenanceRecordUpdateSerializer` 验证数据
- 对 `assistant` 字段进行特殊处理：
  - 如果是空值（`null`、`undefined`、`0`、空字符串），设置为 `None`
  - 如果是有效整数，从**本地数据库**查询用户
  - 验证用户存在、角色是承建方（`manager` 或 `employee`）、且为激活状态
  - 将用户ID转换为用户名存储到 `assistant` 字段

#### 2.3 保存数据
- 更新运维记录到数据库
- 返回更新后的数据（包含 `assistant_display` 字段用于显示）

## 三、关键代码位置

### 后端代码

1. **更新视图：** `backend/apps/maintenance/views.py` - `update()` 方法
2. **更新序列化器：** `backend/apps/maintenance/serializers.py` - `MaintenanceRecordUpdateSerializer`
3. **协助人验证：** `validate_assistant()` 方法
4. **协助人显示：** `get_assistant_display()` 方法

### 前端代码

1. **协助人选择：** `frontend-pc/src/pages/MaintenanceRecordCreate.jsx`
2. **API接口：** `frontend-pc/src/api/users.js` - `getContractors()`

## 四、数据存储

- **assistant字段：** 存储的是用户的 `username`（字符串），不是用户ID
- **assistant_display字段：** 只读字段，用于显示协助人的姓名（优先显示 `full_name`，其次 `username`）

## 五、错误处理

### 1. 验证错误（400）
- 字段格式错误
- 协助人用户不存在或不是承建方用户
- 必填字段缺失

### 2. 服务器错误（500）
- 数据库连接问题
- 其他未预期的异常

所有错误都会记录到日志中，方便排查问题。

## 六、重要确认

✅ **不调用Bspplus接口：** 
- 更新逻辑中完全不涉及Bspplus接口调用
- 所有用户查询都直接从本地数据库进行

✅ **只从承建方选择：**
- 前端下拉列表只显示承建方用户
- 后端验证强制要求协助人必须是承建方用户

✅ **错误处理完善：**
- 区分验证错误（400）和服务器错误（500）
- 记录详细日志，便于排查问题

---

**最后更新日期：** 2026-01-13
