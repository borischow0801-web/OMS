# 运维记录协助人选择逻辑检查报告

## 一、检查结果

✅ **协助人选择逻辑已正确限制为只能从承建方（manager和employee）中选择**

## 二、详细检查

### 1. 前端选择逻辑

#### 1.1 数据源获取
**代码位置：** `frontend-pc/src/pages/MaintenanceRecordCreate.jsx`

```javascript
const loadContractors = async () => {
  setLoadingContractors(true)
  try {
    const response = await userApi.getContractors()  // 调用承建方用户接口
    const users = response.data.results || response.data
    // 过滤掉当前登录用户
    const filteredUsers = users.filter(u => u.id !== user?.id)
    setContractors(filteredUsers)
  } catch (error) {
    console.error('加载承建方用户失败:', error)
  } finally {
    setLoadingContractors(false)
  }
}
```

**API调用：** `userApi.getContractors()` → `/api/accounts/users/contractors/`

**结论：** ✅ 前端调用的是专门的承建方用户接口

#### 1.2 下拉选择组件
**代码位置：** `frontend-pc/src/pages/MaintenanceRecordCreate.jsx`

```javascript
<Select 
  placeholder="请选择协助人" 
  allowClear
  loading={loadingContractors}
  showSearch
  filterOption={(input, option) =>
    (option?.children ?? '').toLowerCase().includes(input.toLowerCase())
  }
>
  {contractors.map(contractor => {
    const roleDisplay = contractor.role === 'manager' ? '项目经理' : contractor.role === 'employee' ? '员工' : contractor.role
    const displayName = contractor.full_name || contractor.first_name || contractor.username
    return (
      <Option key={contractor.id} value={contractor.id}>
        {displayName} ({roleDisplay})
      </Option>
    )
  })}
</Select>
```

**结论：** ✅ 下拉列表只显示 `contractors` 数组中的用户，这些用户来自 `getContractors()` 接口

### 2. 后端接口逻辑

#### 2.1 承建方用户接口
**代码位置：** `backend/apps/accounts/views.py`

```python
@action(detail=False, methods=['get'])
def contractors(self, request):
    """获取承建方用户列表（项目经理和员工）"""
    contractors = User.objects.filter(role__in=['manager', 'employee'], is_active=True)
    serializer = self.get_serializer(contractors, many=True)
    return Response(serializer.data)
```

**过滤条件：**
- ✅ `role__in=['manager', 'employee']` - 只包含项目经理和员工
- ✅ `is_active=True` - 只包含激活的用户

**结论：** ✅ 后端接口严格限制只返回承建方用户

#### 2.2 API路由配置
**代码位置：** `backend/apps/accounts/urls.py`

```python
router = DefaultRouter()
router.register(r'users', UserViewSet, basename='user')
```

由于使用了 `DefaultRouter` 和 `@action` 装饰器，`contractors` 接口会自动注册为：
- URL: `/api/accounts/users/contractors/`
- 方法: GET

**结论：** ✅ 路由配置正确

### 3. 后端验证逻辑

#### 3.1 创建时验证
**代码位置：** `backend/apps/maintenance/serializers.py` - `MaintenanceRecordCreateSerializer`

```python
def validate_assistant(self, value):
    """验证协助人用户ID"""
    # 处理各种空值情况：None、0、空字符串等
    if value is None or value == '' or value == 0:
        return None
    
    # 确保value是有效的整数
    try:
        user_id = int(value)
        if user_id <= 0:
            return None
    except (ValueError, TypeError):
        raise serializers.ValidationError('协助人用户ID格式错误')
    
    from django.contrib.auth import get_user_model
    User = get_user_model()
    try:
        user = User.objects.get(id=user_id, role__in=['manager', 'employee'], is_active=True)
        # 将用户ID转换为用户名存储到assistant字段
        return user.username
    except User.DoesNotExist:
        raise serializers.ValidationError('协助人用户不存在或不是承建方用户')
```

**验证条件：**
- ✅ `role__in=['manager', 'employee']` - 只允许承建方用户
- ✅ `is_active=True` - 只允许激活的用户
- ✅ 如果用户不存在或不符合条件，抛出验证错误

**结论：** ✅ 创建时严格验证协助人必须是承建方用户

#### 3.2 更新时验证
**代码位置：** `backend/apps/maintenance/serializers.py` - `MaintenanceRecordUpdateSerializer`

```python
def validate_assistant(self, value):
    """验证协助人用户ID"""
    # 处理各种空值情况：None、0、空字符串等
    if value is None or value == '' or value == 0:
        return None
    
    # 确保value是有效的整数
    try:
        user_id = int(value)
        if user_id <= 0:
            return None
    except (ValueError, TypeError):
        raise serializers.ValidationError('协助人用户ID格式错误')
    
    from django.contrib.auth import get_user_model
    User = get_user_model()
    try:
        user = User.objects.get(id=user_id, role__in=['manager', 'employee'], is_active=True)
        # 将用户ID转换为用户名存储到assistant字段
        return user.username
    except User.DoesNotExist:
        raise serializers.ValidationError('协助人用户不存在或不是承建方用户')
```

**验证条件：**
- ✅ `role__in=['manager', 'employee']` - 只允许承建方用户
- ✅ `is_active=True` - 只允许激活的用户
- ✅ 如果用户不存在或不符合条件，抛出验证错误

**结论：** ✅ 更新时严格验证协助人必须是承建方用户

### 4. 前端API接口
**代码位置：** `frontend-pc/src/api/users.js`

```javascript
export const userApi = {
  // ... 其他接口
  
  // 获取承建方用户列表（项目经理和员工）
  getContractors: () => api.get('/accounts/users/contractors/'),
}
```

**结论：** ✅ API接口定义正确，调用的是承建方用户接口

---

## 三、总结

### ✅ 正确的限制逻辑

1. **前端选择限制：**
   - 下拉列表只显示从 `/api/accounts/users/contractors/` 接口获取的用户
   - 该接口只返回 `role` 为 `manager` 或 `employee` 的用户

2. **后端接口限制：**
   - `contractors` 接口使用 `User.objects.filter(role__in=['manager', 'employee'], is_active=True)`
   - 严格限制只返回承建方用户

3. **后端验证限制：**
   - 创建和更新时，`validate_assistant` 方法都会验证：
     - 用户必须存在
     - 用户角色必须是 `manager` 或 `employee`
     - 用户必须是激活状态（`is_active=True`）
   - 如果不符合条件，会抛出验证错误

### 🔒 安全保证

即使有人绕过前端直接调用API，后端验证也会拒绝非承建方用户作为协助人。

### 📝 数据存储

协助人字段（`assistant`）存储的是用户的 `username`（字符串），而不是用户ID。这样可以：
- 即使协助人用户被删除，也能保留历史记录
- 显示时通过 `assistant_display` 字段查找用户信息

---

## 四、建议

当前实现已经正确限制了协助人只能从承建方（manager和employee）中选择。建议：

1. ✅ 保持现有实现，无需修改
2. ✅ 定期检查确保没有其他代码绕过这些限制
3. ✅ 在代码注释中明确说明限制逻辑

---

**检查日期：** 2026-01-13
**检查结果：** ✅ 通过 - 协助人选择逻辑已正确限制为只能从承建方选择
