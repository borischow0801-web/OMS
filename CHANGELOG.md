# CHANGELOG.md

本文件基于当前仓库可见代码、迁移和文档整理。早期历史未逐次记录，未确认内容不写入为既成事实。

## Unreleased

### Added

- 新增项目协作文档：
  - `AGENTS.md`
  - `AI_CONTEXT.md`
  - `TASKS.md`
  - `CHANGELOG.md`

### Notes

- 以上文档仅整理当前项目上下文、待办和风险，不修改业务代码。

## 当前仓库已包含的能力

### 后端

- Django REST API。
- 自定义用户模型。
- JWT 登录和刷新。
- Bspplus 统一认证接入。
- Bspplus 用户信息同步。
- Bspplus 网络异常时本地认证回退。
- 用户角色：使用方、管理方、项目经理、员工。
- 用户批量导入 Excel 模板和导入服务。
- 任务管理：
  - 创建任务
  - 保存草稿
  - 提交草稿
  - 审核通过/不通过
  - 指派/重新指派
  - 设置协助员工
  - 开始处理
  - 完成任务
  - 确认完成
  - 退回修改
  - 评论
  - 附件上传、下载、删除
- 工作流日志。
- 站内通知和已读处理。
- 短信配置、模板、发送记录。
- 任务流程短信触发。
- 运维记录：
  - 创建、查看、更新、删除
  - 附件上传、下载、删除
  - 更新内容明细
  - 统计报表

### PC 前端

- 登录与持久化 token。
- token 过期后刷新。
- 角色路由保护。
- 首页统计图表。
- 任务列表、筛选、分页。
- 任务创建、草稿和附件。
- 任务详情及流程操作。
- 通知轮询和未读提醒。
- 个人中心和修改密码。
- 运维记录列表。
- 运维记录创建/编辑/详情。
- 运维记录报表。

### 微信小程序

- 登录页面。
- 首页框架。
- 任务列表。
- 任务详情。
- 附件上传/下载相关代码。

待确认：

- 小程序文档描述的完整流程操作、通知、个人信息等功能是否计划继续实现。

## 数据库迁移摘要

### accounts

- 创建 `users` 表。
- 增加用户 `region_name` 字段。

### tasks

- 创建 `tasks` 表。
- 创建 `comments` 表。
- 增加任务协助员工 ManyToMany 字段。
- 增加 `task_attachments` 表。
- 增加 `draft` 状态。
- 允许任务类型为空。

### workflow

- 创建 `workflow_logs` 表。
- 创建 `notifications` 表。
- 创建短信相关表：
  - `sms_configs`
  - `sms_templates`
  - `sms_records`
- 增加任务需要修改短信模板类型。
- 调整短信配置参数模板字段。

### maintenance

- 创建 `maintenance_records` 表。
- 调整问题类型 choices。
- 将协助人字段调整为 JSONField。
- 创建 `maintenance_attachments` 表。
- 增加完成时间。
- 创建 `maintenance_update_logs` 表。

## 已知不一致

- README 与 `系统说明.md` 未完整覆盖当前代码中的运维记录、短信、Bspplus、附件等能力。
- `docs/Bspplus用户体系调整说明.md` 中角色映射描述与当前代码不完全一致。
- 短信文档中的成功响应字段描述与当前代码判断不完全一致。
- 小程序部署说明中的功能清单多于当前小程序代码体现。
- 部署文档中 `/api/` 根路径验证建议与排障文档“根路径 404 正常”的说明不一致。

