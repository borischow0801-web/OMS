# 运维记录更新失败判断逻辑

## 一、更新失败的判断逻辑

### 1. 权限检查失败
**判断条件：**
- 当前用户角色不是 `manager`（项目经理）或 `employee`（员工）
- 返回状态码：`403 Forbidden`
- 错误信息：`'只有承建方可以更新运维记录'`

**代码位置：**
```python
# backend/apps/maintenance/views.py
def update(self, request, *args, **kwargs):
    if not (request.user.is_manager or request.user.is_employee):
        return Response(
            {'error': '只有承建方可以更新运维记录'},
            status=status.HTTP_403_FORBIDDEN
        )
```

### 2. 协助人字段验证失败

#### 2.1 协助人用户ID格式错误
**判断条件：**
- `assistant` 字段值不是有效的整数
- 返回错误信息：`'协助人用户ID格式错误'`

**代码位置：**
```python
# backend/apps/maintenance/serializers.py
def validate_assistant(self, value):
    try:
        user_id = int(value)
        if user_id <= 0:
            return None
    except (ValueError, TypeError):
        raise serializers.ValidationError('协助人用户ID格式错误')
```

#### 2.2 协助人用户不存在或不是承建方用户
**判断条件：**
- 用户ID对应的用户不存在
- 用户存在但不是 `manager` 或 `employee` 角色
- 用户存在但 `is_active=False`（已禁用）
- 返回错误信息：`'协助人用户不存在或不是承建方用户'`

**代码位置：**
```python
# backend/apps/maintenance/serializers.py
try:
    user = User.objects.get(id=user_id, role__in=['manager', 'employee'], is_active=True)
    return user.username
except User.DoesNotExist:
    raise serializers.ValidationError('协助人用户不存在或不是承建方用户')
```

### 3. 其他字段验证失败

#### 3.1 必填字段缺失
**判断条件：**
- `region`（区划）为空
- `request_date`（需求提出日期）为空
- `issue_type`（问题类型）为空
- `issue_description`（问题描述）为空
- `priority`（优先级）为空
- `completion_status`（完成情况）为空

**错误信息格式：**
- Django REST Framework 会返回字段级别的错误，例如：`{'region': ['这个字段是必填的。']}`

#### 3.2 日期格式错误
**判断条件：**
- `request_date` 或 `expected_completion_time` 格式不正确
- 返回错误信息：`'日期格式错误'` 或类似信息

### 4. 数据库操作失败
**判断条件：**
- 数据库连接失败
- 数据完整性约束违反（外键约束等）
- 返回状态码：`500 Internal Server Error`
- 错误信息：Django 数据库错误信息

### 5. 记录不存在
**判断条件：**
- 要更新的运维记录ID不存在
- 返回状态码：`404 Not Found`
- 错误信息：`'未找到'` 或类似信息

---

## 二、已修复的问题

### 问题：选择协助人后更新失败

**原因分析：**
1. **前端问题：**
   - 当用户清空协助人时，Select组件可能发送 `undefined`、`null`、空字符串 `""` 或 `0`
   - 这些值在后端验证时可能导致错误

2. **后端问题：**
   - 原验证逻辑只检查 `value is not None`，没有处理 `0`、空字符串等情况
   - 如果 `value` 是 `0`，会尝试查找ID为0的用户，导致失败

**修复方案：**

#### 前端修复（`MaintenanceRecordCreate.jsx`）
```javascript
const data = {
  ...values,
  // 确保assistant字段：如果是空值（undefined、null、空字符串、0），则设为null
  assistant: values.assistant && values.assistant > 0 ? values.assistant : null,
}
```

#### 后端修复（`serializers.py`）
```python
def validate_assistant(self, value):
    """验证协助人用户ID"""
    # 处理各种空值情况：None、0、空字符串等
    if value is None or value == '' or value == 0:
        return None
    
    # 确保value是有效的整数
    try:
        user_id = int(value)
        if user_id <= 0:
            return None
    except (ValueError, TypeError):
        raise serializers.ValidationError('协助人用户ID格式错误')
    
    # 验证用户存在且是承建方用户
    # ...
```

#### 错误处理改进
```javascript
catch (error) {
  // 显示详细的错误信息
  const errorMessage = error.response?.data?.error || 
                      error.response?.data?.assistant?.[0] || 
                      error.response?.data?.detail ||
                      error.message ||
                      (isEdit ? '更新失败' : '创建失败')
  message.error(errorMessage)
  console.error('操作失败:', error.response?.data || error)
}
```

---

## 三、更新失败的常见场景

### 场景1：协助人用户已被删除或禁用
- **现象：** 选择协助人后更新失败
- **原因：** 协助人用户可能已被删除或设置为非激活状态
- **解决：** 检查协助人用户是否存在且为激活状态

### 场景2：协助人用户角色变更
- **现象：** 之前可以选择的协助人现在选择后更新失败
- **原因：** 协助人用户角色可能从 `manager`/`employee` 变更为 `user`/`admin`
- **解决：** 确保协助人用户角色是 `manager` 或 `employee`

### 场景3：网络问题
- **现象：** 更新时提示网络错误
- **原因：** 前端与后端连接中断
- **解决：** 检查网络连接，查看后端服务是否正常运行

### 场景4：权限问题
- **现象：** 更新时提示"只有承建方可以更新运维记录"
- **原因：** 当前登录用户不是承建方（manager或employee）
- **解决：** 使用承建方账号登录

### 场景5：必填字段缺失
- **现象：** 更新时提示某个字段是必填的
- **原因：** 表单中必填字段被清空
- **解决：** 确保所有必填字段都有值

---

## 四、调试方法

### 1. 前端调试
```javascript
// 在浏览器控制台查看
console.log('提交的数据:', data)
console.error('错误详情:', error.response?.data)
```

### 2. 后端调试
```python
# 在 views.py 中添加日志
import logging
logger = logging.getLogger(__name__)

def update(self, request, *args, **kwargs):
    logger.info(f"更新运维记录，用户: {request.user.username}, 数据: {request.data}")
    # ...
```

### 3. 检查后端日志
```bash
# 查看后端服务日志
sudo journalctl -u oms-backend -n 100
# 或
tail -f /var/log/oms-backend.log
```

### 4. 使用API测试工具
```bash
# 使用curl测试更新接口
curl -X PATCH http://localhost:8000/api/maintenance/records/1/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "assistant": 2,
    "completion_status": "completed"
  }'
```

---

## 五、错误信息对照表

| 错误信息 | 可能原因 | 解决方法 |
|---------|---------|---------|
| `只有承建方可以更新运维记录` | 用户角色不是manager或employee | 使用承建方账号登录 |
| `协助人用户ID格式错误` | assistant字段值不是有效整数 | 检查前端发送的数据格式 |
| `协助人用户不存在或不是承建方用户` | 用户不存在/角色不对/已禁用 | 检查协助人用户状态 |
| `这个字段是必填的` | 必填字段为空 | 填写所有必填字段 |
| `日期格式错误` | 日期格式不正确 | 检查日期格式 |
| `未找到` | 记录ID不存在 | 检查记录是否存在 |
| `网络错误` | 前后端连接问题 | 检查网络和服务状态 |

---

## 六、预防措施

1. **前端验证：**
   - 在提交前验证所有必填字段
   - 确保assistant字段格式正确（null或正整数）

2. **后端验证：**
   - 严格验证所有输入字段
   - 提供清晰的错误信息

3. **用户体验：**
   - 显示详细的错误信息，帮助用户定位问题
   - 在控制台输出错误详情，方便调试

4. **监控：**
   - 记录更新失败的日志
   - 监控常见错误类型

---

**最后更新日期：** 2026-01-13
