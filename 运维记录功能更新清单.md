# 运维记录功能更新清单

## 一、新增文件列表

### 后端文件（Django）

#### 1. 新建 app：`backend/apps/maintenance/`
```
backend/apps/maintenance/
├── __init__.py                    # 新建
├── admin.py                       # 新建 - Django Admin配置
├── apps.py                        # 新建 - App配置
├── models.py                      # 新建 - 数据模型
├── serializers.py                 # 新建 - 序列化器
├── urls.py                        # 新建 - URL路由
├── views.py                       # 新建 - 视图集
└── migrations/
    ├── __init__.py                # 新建
    ├── 0001_initial.py            # 新建 - 初始迁移（创建maintenance_records表）
    └── 0002_update_issue_type_choices.py  # 新建 - 更新问题类型选项
```

### 前端文件（React）

#### 2. 页面组件：`frontend-pc/src/pages/`
```
frontend-pc/src/pages/
├── MaintenanceRecordList.jsx      # 新建 - 运维记录列表页面
├── MaintenanceRecordCreate.jsx    # 新建 - 运维记录录入/编辑页面
└── MaintenanceReport.jsx          # 新建 - 运维记录报告页面
```

#### 3. API 文件：`frontend-pc/src/api/`
```
frontend-pc/src/api/
└── maintenance.js                 # 新建 - 运维记录API接口
```

---

## 二、修改文件列表

### 后端文件

#### 1. 配置文件
```
backend/oms_backend/settings.py    # 修改 - 在INSTALLED_APPS中添加了'apps.maintenance'
backend/oms_backend/urls.py        # 修改 - 添加了path('api/maintenance/', include('apps.maintenance.urls'))
```

#### 2. 用户相关
```
backend/apps/accounts/views.py      # 修改 - 添加了contractors接口（获取承建方用户列表）
```

### 前端文件

#### 1. 路由和布局
```
frontend-pc/src/App.jsx            # 修改 - 添加了运维记录相关路由和MaintenanceRoute组件
frontend-pc/src/components/Layout.jsx  # 修改 - 添加了运维记录菜单（仅承建方可见）
```

#### 2. API 接口
```
frontend-pc/src/api/users.js       # 修改 - 添加了getContractors()方法
```

---

## 三、数据库变更

### 新增数据表
- `maintenance_records` - 运维记录表

### 数据迁移文件
1. `0001_initial.py` - 创建maintenance_records表
2. `0002_update_issue_type_choices.py` - 更新问题类型选项

---

## 四、部署步骤

### 1. 后端部署步骤

```bash
# 1. 进入后端目录
cd /app/OMS/backend

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 拉取最新代码（确保所有新文件都已上传）

# 4. 运行数据库迁移
python manage.py migrate maintenance

# 5. 收集静态文件（如果需要）
python manage.py collectstatic --noinput

# 6. 重启后端服务
sudo systemctl restart oms-backend
# 或者如果使用gunicorn
sudo systemctl restart gunicorn
```

### 2. 前端部署步骤

```bash
# 1. 进入前端目录
cd /app/OMS/frontend-pc

# 2. 安装依赖（如果有新依赖）
npm install

# 3. 构建生产版本
npm run build

# 4. 重启前端服务（根据实际部署方式）
# 如果使用nginx，可能需要重启nginx
sudo systemctl restart nginx
```

---

## 五、重要注意事项

### ⚠️ 数据库迁移注意事项

1. **备份数据库**
   ```bash
   # 在生产环境执行迁移前，务必先备份数据库
   mysqldump -u用户名 -p数据库名 > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **迁移顺序**
   - 必须先执行 `0001_initial.py`（创建表）
   - 然后执行 `0002_update_issue_type_choices.py`（更新字段选项）
   - 如果表已存在，Django会自动跳过已执行的迁移

3. **迁移检查**
   ```bash
   # 检查迁移状态
   python manage.py showmigrations maintenance
   
   # 如果显示 [X] 表示已执行，[ ] 表示未执行
   ```

### ⚠️ 权限相关注意事项

1. **菜单权限**
   - 运维记录功能仅对承建方（项目经理和员工）可见
   - 使用方和管理方看不到相关菜单
   - 前端和后端都有权限检查

2. **API权限**
   - 所有运维记录API都需要JWT认证
   - 只有承建方用户可以访问
   - 后端会检查用户角色

### ⚠️ 功能特性说明

1. **序号自动生成**
   - 序号字段不在前端显示
   - 创建时自动生成（当前最大序号+1）

2. **处理人自动设置**
   - 处理人字段不在前端显示
   - 创建时自动设置为当前登录用户

3. **协助人字段**
   - 前端显示为下拉选择（承建方用户列表，排除当前用户）
   - 后端存储为用户名（字符串）

4. **问题类型**
   - 已更新为11个选项
   - 需要执行迁移文件 `0002_update_issue_type_choices.py`

### ⚠️ 前端构建注意事项

1. **环境变量**
   - 确保 `.env` 或 `.env.production` 中配置了正确的API地址

2. **路由检查**
   - 新增的路由需要确保在生产环境可以正常访问
   - 检查路由权限是否正确

3. **浏览器缓存**
   - 部署后可能需要清除浏览器缓存
   - 建议使用版本号或时间戳来避免缓存问题

### ⚠️ 测试检查清单

部署后请检查以下功能：

- [ ] 承建方用户登录后能看到"运维记录录入"和"运维记录报告"菜单
- [ ] 使用方和管理方用户看不到运维记录相关菜单
- [ ] 可以正常创建运维记录
- [ ] 可以正常编辑运维记录
- [ ] 可以正常删除运维记录
- [ ] 可以正常查看运维记录列表（带分页）
- [ ] 可以正常查看运维记录报告
- [ ] "提交并新增"功能正常工作
- [ ] 区划下拉选择正常
- [ ] 功能模块下拉选择正常
- [ ] 问题类型下拉选择正常（11个选项）
- [ ] 协助人下拉选择正常（显示承建方用户）
- [ ] 数据库迁移成功执行
- [ ] 序号自动生成正常
- [ ] 处理人自动设置为当前用户

### ⚠️ 回滚方案

如果部署后出现问题，可以按以下步骤回滚：

1. **数据库回滚**
   ```bash
   # 回滚迁移
   python manage.py migrate maintenance 0001_initial
   # 或者完全回滚
   python manage.py migrate maintenance zero
   ```

2. **代码回滚**
   - 恢复修改的文件
   - 删除新增的文件
   - 重启服务

3. **前端回滚**
   - 恢复之前的构建版本
   - 重启前端服务

---

## 六、文件清单总结

### 新增文件（共13个）
- 后端：9个文件（包括迁移文件）
- 前端：4个文件

### 修改文件（共5个）
- 后端：3个文件
- 前端：3个文件

### 总计
- **新增文件**：13个
- **修改文件**：6个
- **总计**：19个文件需要处理

---

## 七、快速部署命令

```bash
# 后端
cd /app/OMS/backend
source venv/bin/activate
python manage.py migrate maintenance
sudo systemctl restart oms-backend

# 前端
cd /app/OMS/frontend-pc
npm run build
# 根据实际部署方式重启前端服务
```

---

**最后更新日期**：2026-01-13
